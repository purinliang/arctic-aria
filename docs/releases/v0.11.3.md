# Release v0.11.3

## Release Title

```text
Release v0.11.3: polish action failures and reminders
```

## Release Text

```text
Release v0.11.3 tightens action failure handling and final reminder polish after v0.11.2.

Action failures now keep shared notification categories while using structured metadata for clearer missing and invalid parameter messages. The release also improves routine reminder Discord text and makes memory add/edit validation follow the visible dialog field order.
```

Use the release title as the PR title and main release commit title. Use the
release text as the PR body and main release commit body.

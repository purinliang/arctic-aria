# Release v0.12.4

## Release Title

```text
Release v0.12.4: polish Today, icons, and form controls
```

## Release Text

```text
Release v0.12.4 polishes Today progress, public app icons, and web form controls after the v0.12.3 Today and project detail release.

The release aligns the Today progress day boundary with the shared local-day schedule. It also regenerates the browser, PWA, and iOS app icon assets from the current public icon design, adds shared control height and form spacing helpers, aligns auth, dialog, picker, choice, and panel action sizing, and places Discord bound-account actions below the account field. The web UI documentation is updated so button, form, and CRUD action sizing stays consistent.
```

Use the release title as the PR title and main release commit title. Use the
release text as the PR body and main release commit body.

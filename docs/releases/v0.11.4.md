# Release v0.11.4

## Release Title

```text
Release v0.11.4: add developer import tools and Today polish
```

## Release Text

```text
Release v0.11.4 adds administrator-only project and routine import tools after the v0.11 reminder work.

Developer Tools can now copy import templates, parse Markdown or JSON drafts, and insert project or routine batches into the signed-in developer account. The release also keeps Today more compact, allows fixed-day routine intervals to be cleared while editing, and keeps the future CLI scaffold as templates only until remote CLI binding is designed.
```

Use the release title as the PR title and main release commit title. Use the
release text as the PR body and main release commit body.
